world_id: fiber-1996
trade: bandwidth
thesis: traffic growth outruns capacity
interval_days: 365
max_age_days: 600
predicates:
  NEED:
    intensity_metric: fiber_intensity
    relevance_metric: fiber_relevant
    min_intensity: 0.5
    circuit:
      op: IF
      args:
      - op: AND
        args:
        - op: GTE
          args:
          - &id001
            metric: fiber_intensity
          - param: min_intensity
        - &id002
          metric: fiber_relevant
      - const: 'TRUE'
      - op: IF
        args:
        - op: OR
          args:
          - op: LT
            args:
            - *id001
            - param: kill
          - op: NOT
            args:
            - *id002
        - const: 'FALSE'
        - const: UNKNOWN
  GAP:
    demand:
    - fiber_demand
    supply:
    - fiber_supply
    threshold: 1.0
    min_sources: 1
    circuit:
      op: IF
      args:
      - op: GTE
        args:
        - op: COUNT
          metrics:
          - fiber_demand
          - fiber_supply
        - param: min_sources
      - op: IF
        args:
        - op: GT
          args:
          - op: LOW
            metric: fiber_demand
          - op: MUL
            args:
            - op: HIGH
              metric: fiber_supply
            - param: threshold
        - const: 'TRUE'
        - op: IF
          args:
          - op: LTE
            args:
            - op: HIGH
              metric: fiber_demand
            - op: LOW
              metric: fiber_supply
          - const: 'FALSE'
          - const: UNKNOWN
      - const: UNKNOWN
  LAG:
    close_metric: fiber_close
    horizon_metric: fiber_horizon
    circuit:
      op: IF
      args:
      - op: GT
        args:
        - &id003
          metric: fiber_close
        - &id004
          metric: fiber_horizon
      - const: 'TRUE'
      - op: IF
        args:
        - op: LTE
          args:
          - *id003
          - *id004
        - const: 'FALSE'
        - const: UNKNOWN
  WTP:
    price_metric: fiber_price
    qty_metric: fiber_qty
    x: 15
    y: 10
    circuit:
      op: IF
      args:
      - op: AND
        args:
        - &id005
          op: GTE
          args:
          - metric: fiber_price
          - param: x
        - op: GT
          args:
          - &id006
            metric: fiber_qty
          - op: SUB
            args:
            - const: 0
            - param: y
      - const: 'TRUE'
      - op: IF
        args:
        - op: AND
          args:
          - *id005
          - op: LTE
            args:
            - *id006
            - op: SUB
              args:
              - const: 0
              - param: y
        - const: 'FALSE'
        - const: UNKNOWN
  NOSUB:
    share_metric: fiber_share
    threshold: 0.25
    redesign_metric: fiber_redesign
    circuit:
      op: IF
      args:
      - op: OR
        args:
        - op: GTE
          args:
          - &id007
            metric: fiber_share
          - param: share_threshold
        - &id008
          metric: fiber_redesign
      - const: 'FALSE'
      - op: IF
        args:
        - op: AND
          args:
          - op: LT
            args:
            - *id007
            - param: share_threshold
          - op: NOT
            args:
            - *id008
        - const: 'TRUE'
        - const: UNKNOWN
