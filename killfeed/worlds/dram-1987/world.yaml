world_id: dram-1987
trade: dram
interval_days: 365
max_age_days: 500
thesis: PC demand outruns DRAM supply
predicates:
  NEED: {intensity_metric: dram_intensity, relevance_metric: dram_relevant, min_intensity: 0.5}
  GAP: {demand: [dram_demand], supply: [dram_supply], threshold: 1.0, min_sources: 1}
  LAG: {close_metric: dram_close, horizon_metric: dram_horizon}
  WTP: {price_metric: dram_price, qty_metric: dram_qty, x: 15, y: 10}
  NOSUB: {share_metric: dram_share, threshold: 0.25, redesign_metric: dram_redesign}
